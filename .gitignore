# --- OS / Editors ---
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# --- Ruby / Bundler ---
/.bundle/
/vendor/bundle/
.byebug_history
coverage/

# --- Rails logs & temp ---
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
/tmp/pids/*
/tmp/sockets/*
/tmp/cache/*

# --- Active Storage (local dev only) ---
/storage/*
!/storage/.keep

# --- Assets / JS package managers ---
/node_modules/
yarn-error.log
npm-debug.log*
pnpm-debug.log*

# Rails precompiled assets (you build these in Docker/ECS)
/public/assets
/public/packs
/public/packs-test

# --- Environment & credentials (DO NOT COMMIT) ---
.env
.env.*
/config/master.key
/config/credentials/*.key

# If your database.yml has any secrets, ignore it and keep a sample checked in:
# (create config/database.yml.example and commit that instead)
#/config/database.yml

# --- Docker (keep Dockerfile and .dockerignore tracked) ---
.docker/
docker-compose.override.yml

# --- Keys / certificates accidentally dropped in the repo root ---
*.pem
*.p12
*.crt
*.key

# --- macOS / Windows metadata again (just in case in subfolders) ---
**/.DS_Store
**/Thumbs.db
aws/
