<% content_for :title, "New incident" %>

<h1 class="text-2xl font-semibold mb-4">New incident</h1>

<%= render "form", incident: @incident %>

<div class="mt-3">
  <%= link_to "Back",
              (@asset ? asset_incidents_path(@asset) : asset_path(@incident.asset)),
              class: "btn btn-ghost" %>
</div>
